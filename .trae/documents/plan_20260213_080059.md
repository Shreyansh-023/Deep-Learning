I will generate a complete `lab4.ipynb` notebook implementing the following detailed steps to satisfy all lab requirements:

1.  **Setup & Utilities**:
    *   Install/Import libraries (`torch`, `torchvision`, `sklearn`, `seaborn`, `tqdm`).
    *   Define `set_seed()` for reproducibility and `get_device()` for GPU acceleration.
    *   Implement **visualization helpers** to plot loss curves and confusion matrices.

2.  **Dataset Implementation (Problem Statements 1 & 2)**:
    *   **Imbalanced CIFAR-10**: Create a custom dataset class that downloads CIFAR-10 and applies exponential decay sampling (Imbalance Ratio 100:1).
    *   **Flower Dataset**: Implement a downloader for the "Flower Photos" dataset (5 classes) and wrap it in `torchvision.datasets.ImageFolder`.
    *   **Imbalance Handling**:
        *   **Data-Level**: Implement `WeightedRandomSampler` to oversample minority classes.
        *   **Algorithm-Level**: Compute inverse class frequencies to create **Class Weights** for the loss function.

3.  **Model Architectures (Problem Statements 1 & 3)**:
    *   **ResNet18**: Use as the primary model (pretrained), modifying the final layer for dataset-specific classes.
    *   **MobileNetV2**: Use as the comparison model to analyze efficiency vs performance.

4.  **Loss Functions (Problem Statement 4)**:
    *   **CrossEntropy**: Standard baseline.
    *   **Focal Loss**: Implement a custom `FocalLoss` module to penalize hard-to-classify examples.

5.  **Experiment Pipeline (Running the Lab)**:
    *   **Experiment A (Baseline)**: Train ResNet18 on Imbalanced CIFAR-10 with standard CrossEntropy.
    *   **Experiment B (Oversampling)**: Train with `WeightedRandomSampler`.
    *   **Experiment C (Class Weighting)**: Train with Weighted CrossEntropy.
    *   **Experiment D (Focal Loss)**: Train with Focal Loss.
    *   **Experiment E (Architecture Comparison)**: Train MobileNetV2 on the imbalanced data.
    *   **Experiment F (Second Dataset)**: Train ResNet18 on the Flower dataset to meet the "Two Datasets" requirement.

6.  **Analysis & Visualization (Problem Statements 5 & 7)**:
    *   Calculate and display **Precision, Recall, F1-score**.
    *   Plot **Confusion Matrices** for the best and worst performing models.
    *   (Code included) Extract features and visualize using **t-SNE**.

The notebook will be fully executable, automatically handling data downloading and training.